# Fig1 代码一致性人工核对报告

## 核对对象

- 资产：`manufacture/mosaic_n_bioinformatics_manuscript_v1/oup-authoring-template/Fig/fig1_user_final.png`
- 正文引用：`main.tex` 的 Figure 1 caption
- 核对日期：2026-08-16

## 已核对内容

1. 图中保留 paired RNA/ADT 输入，并明确标出 3,000 genes、224 proteins 和相同细胞配对。
2. RNA 与 ADT 分支均显示独立编码、256 维 embedding 和 58 类 L3 head。
3. Fusion 区域显示 512 维拼接、fusion MLP 和 256 维融合 embedding。
4. Reliability arbitration 区域显示三组 branch logits、top-two margins、learned biases、softmax 权重和 fused base logits。
5. HSR 区域显示 delta head、gate head、314 维输入、六个预声明 L3 sibling labels、bounded update 和 52 个不变标签。
6. Cell-level audit record 区域显示 before/after logits、gate/confidence、delta norm、branch outputs 和 no-global-relabeling 结果。

## 状态边界

以上是对用户提供图像中可见文字、维度和模块的人工一致性核对，图像文件未被修改。箭头和局部连线仍属于绘图资产本身，不能由 LaTeX 或 Python 自动证明；最终上传前仍需作者对照模型代码再做一次视觉确认。
