# V33 synthetic smoke-test fixture

`v33_synthetic_smoke.csv` is deterministic synthetic data for checking the
release environment, CSV schema, donor-disjoint split handling, train-only
standardization and metric execution. It contains no participant-derived
measurements and must not be interpreted as scientific evidence or as a
substitute for the accessioned datasets used in the manuscript.
